class CfgSettings {
    class CBA {
        class Versioning {
            class CavMod {
                class dependencies {
                    //Mod will hard exit if this is missing
                    CBA[] = {"cba_main", REQUIRED_CBA_VERSION, "(true)"};
                };
            };
        };
    };
};
